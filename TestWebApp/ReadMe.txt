Because the Visual Studio Debugger (casini) does not support host header binding 
you can't test subdomain things using it. You will need to create a local IIS app 
with appropriate bindings to test it.